version: '3.1'

services:

  api:
    build: .
    restart: always
    ports:
      - 80:8080
    depends_on:
      - db

  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: finances-api